<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="../css/reset.css" />
  <link rel="stylesheet" href="../css/infoPage.css">
  <link rel="stylesheet" href="../css/footer.css">
  <link rel="stylesheet" href="../css/header.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
  <title>Character Page</title>
</head>

<body>

  <%- include('partials/header.ejs') %>

  <main>
    <h2 class="itemTopName">
      <%=character.name%>
    </h2>
    <br>
    <section class="proberen">
      <section class="avatarInfoFav">
        <img src="<%= character.images %>" class="profile__image" />
        <div class="profile__details">
          <div class="infoAvatars">
            <% if(character.description){ %>
              <p><span>Description:</span>
                <%= character.description %>
              </p>
              <% } %>
                <% if(character.type){ %>
                  <p><span>Introduction:</span>
                    <%= character.introduction %>
                  </p>
                  <% } %>
                    <% if(character.rarity){ %>
                      <p><span>Rarity:</span>
                        <% if (character.rarity=="EFortRarity::Epic" ) { %>
                          <p>Epic</p>
                        <% } else if (character.rarity=="EFortRarity::Legendary" ) { %>
                          <p>Legendary</p>
                        <% } else if (character.rarity=="EFortRarity::Uncommon" ) { %>
                          <p>Uncommon</p>
                        <% } else if (character.rarity=="EFortRarity::Common" ) { %>
                          <p>Common</p>
                        <% } else if (character.rarity=="EFortRarity::Rare" ) { %>
                          <p>Rare</p>
                        <% } %>  
                      </p>
                      <% } %>
          </div>
          <div class="infoForm">
            <form action="/favoriet/<%= character.name %>/update" method="post">
              <input type="hidden" name="name" value="<%=character.name%>">
              <input type="hidden" name="image" value="<%=character.images%>">
              <label for="wins" class="textForm"><i id="thumbs-up" class="fa fa-thumbs-up"></i></label>
              <input type="number" id="wins" name="wins" min="0" max="5" placeholder="0" value="<%= character.wins || 0 %>">
              <label for="loses" class="textForm"><i id="thumbs-down" class="fa fa-thumbs-down"></i></label>
              <input type="number" id="loses" name="loses" min="0" max="5" placeholder="0" value="<%= character.loses || 0 %>"><br><br>
              <label class="titleForm" for="notitieAvatar">Notes</label><br><br>
              <textarea name="notitieAvatar" id="notitieAvatar" cols="30" rows="10" placeholder="<%= character.notitieAvatar %>"></textarea></textarea><br><br>
              <div class="formSendButton">
                <input type="reset" value="Reset">
                <input type="submit" value="Submit">
              </div>
            </form>
          </div>
        </div>
      </section>
    </section>
    <br><br><br>
    <h1 class="itemTitle">Shop</h1>
    <br>
    <div class="charTabel">
      <form action="/favoriet/<%= character.name %>/update" method="post">
        <input type="hidden" name="id" value="<%=character.name%>">
        <section class="testing">
          <% for(let i=0; i<4;i++){ %>
            <div class="charSolo">
              <div class="TitleInputForm">
                <h2 class="itemTitleh2">
                  <%= avatarBackpack[i].name %>
                </h2>
                <input type="radio" name="backpack" value="<%= avatarBackpack[i].icon %>">
              </div>
              <label for="backpack" name="<%=i%>"><img src="<%=avatarBackpack[i].icon %>" /></label> <br>
              <div class="TitleInputForm">
                <h2 class="itemTitleh2">
                  <%= avatarPickaxe[i].name %>
                </h2>
                <input type="radio" name="pickaxe" value="<%= avatarPickaxe[i].icon %>">
              </div>
              <label for="pickaxe" name="<%=i%>"><img src="<%= avatarPickaxe[i].icon %>" /></label>
            </div>
            <% } %>
        </section>
        <br>
        <div class="formSendButton">
          <input type="reset" value="Reset">
          <input type="submit" value="Submit">
        </div>
      </form>
    </div>
    </section>
    </div>
  </main>
  <%- include('partials/footer.ejs') %>
